[package]
name = "hpke-rs"
version = "0.6.1+discrypt.1"
authors = ["Franziskus Kiefer <franziskuskiefer@gmail.com>"]
edition = "2021"
license = "MPL-2.0"
documentation = "https://docs.rs/hpke-rs"
description = "HPKE Implementation"
readme = "Readme.md"
repository = "https://github.com/cryspen/hpke-rs"

[dependencies]
hpke-rs-crypto = { version = "0.6.1", default-features = false }
hpke-rs-libcrux = { path = "../hpke-rs-libcrux-0.6.1-discrypt", version = "0.6.1", optional = true, default-features = false }
hpke-rs-rust-crypto = { version = "0.6.1", optional = true, default-features = false }
libcrux-sha3 = "0.0.10"
log = "0.4"
rand_core = { version = "0.9", default-features = false }
serde = { version = "1.0", features = ["derive"], optional = true }
subtle = { version = "2.6", default-features = false }
tls_codec = { version = "0.4.2", features = ["derive"], optional = true }
zeroize = { version = "1.5", features = ["zeroize_derive"] }

[features]
default = []
experimental = ["hpke-rs-rust-crypto?/experimental"]
hazmat = []
hpke-test = ["std"]
hpke-test-prng = [
    "hpke-rs-rust-crypto?/deterministic-prng",
    "hpke-rs-libcrux?/deterministic-prng",
]
libcrux = ["dep:hpke-rs-libcrux"]
rustcrypto = ["dep:hpke-rs-rust-crypto"]
serialization = ["serde", "tls_codec", "tls_codec/serde", "std"]
std = [
    "rand_core/std",
    "hpke-rs-crypto/std",
    "hpke-rs-rust-crypto?/std",
    "hpke-rs-libcrux?/std",
]

[lib]
name = "hpke_rs"
path = "src/lib.rs"

[workspace]
