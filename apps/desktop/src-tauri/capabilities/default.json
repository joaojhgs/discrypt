{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-app-events",
  "description": "Allow the main Discrypt window to subscribe to backend app-state events.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
